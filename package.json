{
  "name": "price-charming",
  "version": "0.1.0",
  "description": "A fairy-tale themed auto-battler card game",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node scripts/copy-data.js",
    "test": "jest --verbose",
    "gen-notes": "node scripts/gen-patch-notes.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  }
}
